import { UserEntity } from './UserEntity';

export interface PostEntity {
  id: number;
  title: string;
  body: string;
  tags: string[];
  reactions: {
    likes: number;
    dislikes: number;
  };
  views: number;
  userId: UserEntity['id'];
}
